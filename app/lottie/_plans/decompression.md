* Create a decompressor and compressor for converting to and from JSON + Lottie files
* Verify with tests against the files in [assets folder](../_assets), they are the same project exported in the different format
* .lot is just the uncompressed JSON format with a new extension, so doesn't need to be handled as a seperate case