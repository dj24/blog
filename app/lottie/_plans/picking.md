Picking will be done by rendering one frame with each primitive's bounding box.

The colour output will be the primitive's id hashed into the red green and blue colour channels, giving a maximum supported object count of 16,777,215 (24bit)

alpha channel will be 0 for no hit.